

/*if(typeof Blueprint === "undefined") {
  var Blueprint = require("../src")
}

describe("Sessions", function() {

  var prefix = Date.now().toString()

  before(function(done) {
    // Setup
    Blueprint.Init({
    	application_id: "5543850719b6366c23000001",
      port: 8080
    })

    // Create a user
    Blueprint.Register({
      email: prefix + "_b@test.goblueprint.co",
      password: "1234567890a",
      name: prefix + " Test User B"
    }).then(function(error, user){
      done()
    })
  })

  it("Can Get Session Info", function(done) {

  })

  it("Can Destroy Session", function(done) {

  })

  it("Can Restore Session from Store", function(done){

  })
});
 */

(function() {


}).call(this);
